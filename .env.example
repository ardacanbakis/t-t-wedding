# Admin dashboard password (required in production)
ADMIN_PASSWORD=change-me

# Optional: point at a hosted libSQL/Turso database instead of the local
# SQLite file (data/wedding.db). Leave unset for self-hosting.
# DATABASE_URL=libsql://your-db.turso.io
# DATABASE_AUTH_TOKEN=...
